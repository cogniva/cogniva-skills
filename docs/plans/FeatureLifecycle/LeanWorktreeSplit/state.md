# LeanWorktreeSplit — execution state

Status: in-progress
Target branch: main
Worktree: C:\WorkingGit\cogniva-skills-lean-worktree-split
Integration: not started

## Sub-plans
- [x] 01-SwitchAndHooks
- [x] 02-ExecuteFeature
- [x] 03-PlanFeatureQuickFix
- [x] 04-PeripheryAndDocs

## Log
- 2026-08-19 01-SwitchAndHooks Task 1 (bdbfa2c): +plugins/cogniva-dev/docs/worktrees.md, +docs/adr/0026-per-clone-worktree-switch.md (ADR-C1 dereferenced to 0026); gitignore template now ignores .claude/cogniva-dev.local.json; repo README template reworded to "config home, not switch".
- 2026-08-19 01-SwitchAndHooks Task 2 (c5721c7): guard-primary-edit.js, guard-primary-git.js, nudge-backlog-commit.js now gate on a shared worktreesOn(topo) predicate reading .claude/cogniva-dev.local.json instead of the .claude/cogniva-dev/ directory; predicate duplicated per script (no shared require) to keep each hook standalone; verified with a throwaway stdin harness (6 red -> 6 green) plus node --check.
- 2026-08-19 02-ExecuteFeature Task 1 (810b3f4): plugins/cogniva-dev/skills/execute-feature/SKILL.md fully replaced with the lean body (worktree dispatch on .claude/cogniva-dev.local.json, new Step 1 plan normalization, ⟦worktree⟧ tags, green gate last); +docs/adr/0027-plan-normalization-step.md (ADR-C3 dereferenced to 0027); body extracted verbatim from the sub-plan fence (roundtrip-diffed); claude plugin validate + check-adrs both clean.
- 2026-08-19 02-ExecuteFeature Task 2 (329ad31): +plugins/cogniva-dev/skills/execute-feature/WORKTREE.md (verbatim overlay: replaces Step 0 with new-feature-worktree.ps1 + staleness gate, Steps 1-4 qualifiers incl. optional statePath and Push-Location green gate, Step 4.6 landing = integrate-feature.ps1 -> mark-cleanupable.ps1); +docs/adr/0028-worktree-overlay-pattern.md (ADR-C2 dereferenced to 0028); content extracted from the sub-plan fence and roundtrip-diffed; check-adrs + claude plugin validate both clean.
- 2026-08-19 02-ExecuteFeature Task 3 (2c453b0): plugins/cogniva-dev/templates/execute-feature.workflow.js now takes `workspace` (legacy alias `worktree`) resolved off `_args` (NOT raw `args`, which may arrive as a JSON string) and the prompt is mode-neutral ("Your only working directory is: <workspace>"); statePath/planPath instructions were already conditional, header args doc now says statePath is OPTIONAL; verified with a throwaway harness that executes the template against fake runtime globals (3 red -> 9 green). Step 5 caveat: the literal `node --check` cannot exit 0 for this file (ESM `export` + top-level `return`; fails identically on HEAD) — ran the equivalent parse of the export-stripped, async-wrapped body instead, exit 0. Seam found for followup: lean SKILL.md passes `branch`, the template reads `featureBranch`.
- 2026-08-19 02-ExecuteFeature Task 4 (877120b): plugins/cogniva-dev/scripts/check-adrs.ps1 gained a lean -Workspace/-Since mode - all four checks now run off $root/$compareRef/$base, with the diff base being the merge-base (worktree mode) or the resolved -Since commit (lean mode), and check D comparing against the tree at -Since. Modes are validated BY HAND rather than by a PowerShell ParameterSetName, because a set-resolution failure exits 1 and 1 is reserved for "problems found" - every usage mistake now exits 2. plugins/cogniva-dev/tests/check-adrs/check-adrs.tests.ps1 extended: Run-Raw/Run-Since/Head-Sha helpers plus 17 new assertions (clean -Since pass, pre-Since label ignored, leaked ADR-Cn, reused number, opt-out marker, six usage cases); also replaced `git init -b main` with init + symbolic-ref so the suite runs on git < 2.28 (this box has 2.26 and the suite could not start). 36/36 green; claude plugin validate clean; both modes sanity-run against this worktree.
- 2026-08-19 03-PlanFeatureQuickFix Task 1 (2ee9bbe): plugins/cogniva-dev/skills/plan-feature/SKILL.md fully replaced with the lean body (worktree dispatch on .claude/cogniva-dev.local.json, ⟦worktree⟧ tags around plan emission + commit, no state.md seeding in lean mode); plugins/cogniva-dev/skills/plan-feature/PLAN-FORMAT.md companion-state line now says state.md is seeded IN WORKTREE MODE only; body extracted from the sub-plan fence and roundtrip-diffed against the committed blob (identical); claude plugin validate clean. No Candidate ADRs section in this sub-plan, so no ADR written.
- 2026-08-19 03-PlanFeatureQuickFix Task 2 (60ee1a1): +plugins/cogniva-dev/skills/plan-feature/WORKTREE.md (verbatim overlay: authoring the plan folder on a new-feature-worktree.ps1 worktree, the state.md seed template that lean mode drops, and integrate -> mark-cleanupable -> cleanup-worktrees run IMMEDIATELY from the primary checkout so execute-feature cannot silently reuse the planning worktree); content extracted from the sub-plan fence and roundtrip-diffed (identical); claude plugin validate + check-adrs clean. No Candidate ADRs section in this sub-plan, so no ADR written.
- 2026-08-19 03-PlanFeatureQuickFix Task 3 (14e9a4a): plugins/cogniva-dev/skills/quick-fix/SKILL.md fully replaced with the lean body (worktree dispatch on .claude/cogniva-dev.local.json, Step 0 workspace = repo root + START sha, Step 0.5 candidate ADRs confirmed before dispatch, Step 2 land order ending at the green gate, ⟦worktree⟧ tags on Step 0 / the ADR check / done); content extracted from the sub-plan fence by script and roundtrip-diffed against the file (identical, 66 lines); written CRLF to match the repo checkout convention (.gitattributes text=auto), blob normalizes to LF; claude plugin validate clean. No Candidate ADRs section in this sub-plan, so no ADR written.
- 2026-08-19 03-PlanFeatureQuickFix Task 4 (c1ad1ba): +plugins/cogniva-dev/skills/quick-fix/WORKTREE.md (verbatim overlay: replaces Step 0 with new-feature-worktree.ps1 + slug derivation + staleness gate, and replaces Step 2's ADR check / done with check-adrs -Worktree/-TargetBranch, a Push-Location-bracketed green gate, then integrate-feature.ps1 -> mark-cleanupable.ps1 with hand-off to /cleanup-work); content extracted from the sub-plan fence by an awk de-indenter and roundtrip-diffed against the file (identical, 34 lines); written CRLF to match the sibling overlays (.gitattributes text=auto), blob normalizes to LF; claude plugin validate + check-adrs both clean. No Candidate ADRs section in this sub-plan, so no ADR written.
- 2026-08-19 04-PeripheryAndDocs Task 1 (0641b3d): plugins/cogniva-dev/skills/cleanup-work/SKILL.md and plugins/cogniva-dev/skills/cleanup-allwork/SKILL.md each gained the verbatim `## Mode check (first)` short-circuit block; placed as the FIRST `##` section (after the H1 + intro/Invoke/`<plugin>` lines) so the Invoke and plugin-root lines stay in the intro rather than being swallowed by the new section; block extracted from the sub-plan fence by script and byte-compared (no hand-typed em dash), written CRLF to match the files (.gitattributes text=auto); check re-run red -> green, claude plugin validate clean. No Candidate ADRs section in this sub-plan, so no ADR written.
- 2026-08-19 04-PeripheryAndDocs Task 2 (426ac9f): plugins/cogniva-dev/skills/backlog/SKILL.md (Step 2 ride-along, Step 4a write root, Step 7 commit rule + -RepoPath, non-interactive-caller bullet, Rules routing bullet), plugins/cogniva-dev/skills/explore-idea/SKILL.md (worktree-mode clause in "Where the thinking lives" + closing section replaced verbatim with `## Note for worktree mode`), plugins/cogniva-dev/skills/groom-backlog/SKILL.md (Step 4 apply mechanics) all reworded mode-neutrally: writes land "in your workspace (the worktree you are working in, when one is active; otherwise the checkout)", and the tier-1 primary-capture exemption is now explicitly conditioned on worktree mode. groom-backlog had ZERO literal `worktree` hits, so Step 3 was applied to the equivalent primary-edit-guard / "guard-opted repo" wording per its parenthetical. Capture-bar semantics (Tests 1-3, the three-section gate, append-by-default) untouched - asserted by invariant checks. Edits made by a line-range splice script with anchor assertions (CRLF preserved); explore-idea note extracted byte-exact from the sub-plan fence; grep check run red (15 assertions, 11 fail on HEAD blobs) -> green; claude plugin validate clean. No Candidate ADRs section in this sub-plan, so no ADR written.
- 2026-08-19 04-PeripheryAndDocs Task 3 (703c316): README.md gained the verbatim two-modes paragraph (extracted byte-exact from the sub-plan fence, written CRLF to match the file) between the cogniva-dev pieces table and `### Install into any repo`; plugins/cogniva-dev/.claude-plugin/plugin.json description now reads "with optional per-clone isolated-worktree execution and ledger-based cleanup" (version deliberately left at 0.5.0 - the bump is offered at integration and must move marketplace.json in the same commit). Step 3's BACKLOG.md write was deliberately NOT performed: a task agent inside a Workflow writes nothing to BACKLOG.md (backlog SKILL.md non-interactive-caller rule + the standing task-agent instruction), so the module-deps `-Check` line is returned verbatim in this task's `followups` for the console to gate - receipt: plugins/cogniva-dev/skills/module-deps/module-deps.ps1 param block has only -RepoRoot/-OutFile/-HtmlFile/-Open/-NoCommit. Verification script run red (4 of 8 assertions failing) -> green; claude plugin validate and check-adrs -Workspace . -Since HEAD both clean. No Candidate ADRs section in this sub-plan, so no ADR written.
