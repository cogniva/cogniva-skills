# CodexDualHost — execution state

Status: integrated
Target branch: feature/lean-worktree-split
Worktree: C:/WorkingGit/cogniva-skills-codex-dual-host (branch feature/codex-dual-host)
Integration: not started

## Log
- Task 1 (5290e2d): created plugins/cogniva-dev/.codex-plugin/plugin.json (identity fields copied verbatim from .claude-plugin, version 0.6.2 not 0.6.1 as the plan header assumed, plus "skills": "./skills/") and plugins/cogniva-dev/docs/codex.md (Packaging / Install under Codex / Backend selection / Lean policy / Resume under Codex). `claude plugin validate .` passes.
- Task 2 (42de4f8): created plugins/cogniva-dev/skills/execute-feature/CODEX.md (sequential subagent loop replacing SKILL.md Step 3; DONE/BLOCKED first-line contract; executor ticks checkboxes, task agents do not) and docs/adr/0029-dual-host-packaging-and-codex-overlay.md (candidate copied verbatim, next free number 0029); modified execute-feature SKILL.md (Host dispatch paragraph) and WORKFLOW-NOTES.md (Claude-backend scope sentence + AsyncFunction syntax-check hint, since node --check cannot parse the workflow DSL). `claude plugin validate .` passes.
- Task 3 (8cc4cc7): added `## Flags` to execute-feature SKILL.md (commits=none|task|final, plan=ephemeral|persisted, lean vs worktree defaults + the worktree rejection of none|final) and threaded the policy through Steps 0a/1/3/4.1; CODEX.md now writes the resolved commit rule VERBATIM into each subagent prompt; templates/execute-feature.workflow.js takes `args.commits` (default 'task') and, under task, ticks checkboxes BEFORE the commit while none/final leave the tree dirty; wrote docs/adr/0030-lean-execution-policy.md (candidate copied verbatim, next free number 0030). Decisions: SKILL.md Step 3 now passes `commits` in the Workflow args (otherwise the flag can never reach the template); under commits=task the Codex task agent ticks its own checkboxes so the SHA is accurate, and the executor-ticks rule applies to none/final. Verify: the WORKFLOW-NOTES.md AsyncFunction one-liner fails on this file even at HEAD (`export const meta` is ESM) — compiled with `^export ` stripped instead, exit 0; `claude plugin validate .` passes.
- Task 4 (fc956d0): created plugins/cogniva-dev/skills/execute-feature/HANDOFF.md (READY FOR REVIEW header block + all 13 required sections, "say none, never drop" rule, closing semantics line); SKILL.md Step 4 gained `git diff --check` as new item 5 between the ADR check and the green gate (green gate → 6) and item 7 "Done — the handoff" now emits the full HANDOFF.md handoff instead of a 2–4 sentence report; CODEX.md's "After the loop" points at the same finish. Decision: renumbering Step 4 made WORKTREE.md's "Replaces Step 4.6 Done" stale, so that one heading was retargeted to 4.7 in the same commit. Verify: `claude plugin validate .` passes; `git diff --check` clean.
- Task 5 (ea7457e): modified plugins/cogniva-dev/skills/quick-fix/SKILL.md only — added the **Host dispatch (check second)** paragraph (Workflow tool absent → follow `../execute-feature/CODEX.md`'s loop over the synthesized task list; worktree mode unsupported without the Workflow runtime) and a **Flags** paragraph (quick-fix honours `commits=` with execute-feature's semantics/defaults, none|final invalid in worktree mode; planless so `plan=` does not apply); Step 2's landing order gained `git diff --check` between the ADR check and the green gate, and the lean ending now emits the full handoff per `../execute-feature/HANDOFF.md` instead of a 1–2 sentence report. Decision: Step 1's unconditional "stages only its own files, and commits" was qualified by the commit policy (it otherwise contradicts the new lean default) using policy wording rather than a literal flag token, so the verify grep for `commits=` stays at exactly one hit. Verify: `claude plugin validate .` passes; HANDOFF.md / diff --check / commits= each grep to exactly 1.
- Task 6 (45b3481): added the lean-only **Branch policy** paragraph to execute-feature SKILL.md Step 0b and quick-fix SKILL.md Step 0 (read `.claude/cogniva-dev/policy.json`, check `requiredDevelopmentBranchPrefix` BEFORE any mutation, stop-never-switch, absent/unreadable = no policy); documented the file in plugins/cogniva-dev/templates/repo/.claude/cogniva-dev/README.md (new `## Branch policy` section) plus a one-line rationale in its worktree paragraph; emptied the deny list in plugins/cogniva-dev/templates/repo/.claude/settings.json; repo-init SKILL.md step 3 now states the guard-hook rationale and mentions the optional policy.json; wrote docs/adr/0031-repo-branch-policy.md (candidate ADR-C3 verbatim, next free number 0031). Decision on the Step 3 precondition: `guard-primary-git` is NOT registrable in a repo settings.json — it is registered by the plugin itself in plugins/cogniva-dev/hooks/hooks.json (PreToolUse/Bash) and `${CLAUDE_PLUGIN_ROOT}` does not resolve outside a plugin manifest, so adding a copy to the template would ship a broken hook; the registration was verified live instead (the hook blocked a grep in this session) and the denies were then removed. Verify: `claude plugin validate .` passes; template settings.json parses (`json ok`); zero branch-switch deny hits; no `ADR-C3` label survives.
- Task 7 (3a132c6): added a `> **Scope — Claude Workflow journals only.**` blockquote under the heading of plugins/cogniva-dev/skills/workflow-status/SKILL.md (report "Claude Workflow runs only — Codex runs are not tracked here" under a host with no Workflow runtime); set `"version": "0.7.0"` in plugins/cogniva-dev/.claude-plugin/plugin.json, plugins/cogniva-dev/.codex-plugin/plugin.json and the cogniva-dev entry of .claude-plugin/marketplace.json; rewrote CLAUDE.md's "**A bump is TWO files, always.**" rule as THREE files, adding the `.codex-plugin/plugin.json` path to both the prose and the confirmation grep. Decision: the plan header said 0.6.1 → 0.7.0 but the branch was already at 0.6.2 — 0.7.0 is an absolute target, so all three fields were set to it and the cogniva-skills entry (0.7.2) was left untouched. Verify: the three-file grep reads 0.7.0/0.7.0/0.7.0; `claude plugin validate .` passes.
