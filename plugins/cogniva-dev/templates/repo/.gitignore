# .NET
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/
artifacts/
TestResults/
*.binlog

# Node (Blazor tooling)
node_modules/

# OS
Thumbs.db
.DS_Store

# cogniva-dev worktree workflow: disposable scratch the AI may write in the
# primary checkout (never committed, so it cannot dirty your branch)
.explore/
.plans-staging/
